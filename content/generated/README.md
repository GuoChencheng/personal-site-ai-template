# Generated Content

Optional publish workflows can write `site-content.json` in this directory.

The source TypeScript personas in `content/personas/` remain the default canonical content. If a project chooses to load generated JSON in a future batch, keep the file shaped as `PersonaContent`, review it before merging, and avoid committing private editor drafts or secrets.
